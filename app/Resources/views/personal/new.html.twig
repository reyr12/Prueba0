{% extends 'base.html.twig' %}

{% block body %}
    <h1>Personal creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" style="text-transform:uppercase;" onkeyup="javascript:this.value=this.value.toUpperCase();"/>
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('personal_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
